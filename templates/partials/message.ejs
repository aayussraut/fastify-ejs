<% if (message) { %> <!-- Check if the `message` variable exists -->
    <div class="flex justify-center bg-red-600 p-2 font-semibold text-white mb-2 ">
        <%= message %> <!-- Output the value of the `message` variable -->
    </div>
    <% } %>